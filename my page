<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>XAT3LMAG8C | Ahmed Mohamed</title>

    <meta name="description"
          content="XAT3LMAG8C - Ahmed Mohamed | Cybersecurity Portfolio">

    <style>

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html {
            scroll-behavior: smooth;
        }

        body {
            background: #020303;
            color: #d7ffd7;
            font-family: "Courier New", monospace;
            overflow-x: hidden;
        }

        /* MATRIX BACKGROUND */

        #matrix {
            position: fixed;
            inset: 0;
            z-index: -2;
            opacity: 0.13;
        }

        body::after {
            content: "";
            position: fixed;
            inset: 0;
            pointer-events: none;
            background:
                repeating-linear-gradient(
                    0deg,
                    rgba(0,255,100,0.025),
                    rgba(0,255,100,0.025) 1px,
                    transparent 1px,
                    transparent 4px
                );
            z-index: 10;
        }

        /* NAVBAR */

        nav {
            position: fixed;
            top: 0;
            width: 100%;
            padding: 18px 8%;
            display: flex;
            justify-content: space-between;
            align-items: center;
            background: rgba(0,0,0,0.75);
            backdrop-filter: blur(10px);
            border-bottom: 1px solid rgba(0,255,100,0.15);
            z-index: 20;
        }

        .logo {
            color: #00ff66;
            font-size: 20px;
            font-weight: bold;
            text-shadow: 0 0 12px #00ff66;
        }

        nav a {
            color: #aaa;
            text-decoration: none;
            margin-left: 20px;
            transition: 0.3s;
        }

        nav a:hover {
            color: #00ff66;
            text-shadow: 0 0 10px #00ff66;
        }

        /* HERO */

        .hero {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            padding: 100px 20px 50px;
        }

        .hero-content {
            max-width: 900px;
        }

        .terminal {
            color: #00ff66;
            font-size: 15px;
            margin-bottom: 25px;
        }

        .hero h1 {
            font-size: clamp(45px, 9vw, 100px);
            color: #fff;
            text-shadow:
                0 0 10px #00ff66,
                0 0 30px rgba(0,255,100,0.5);
            letter-spacing: 5px;
        }

        .codename {
            margin-top: 15px;
            font-size: clamp(22px, 4vw, 38px);
            color: #00ff66;
            letter-spacing: 8px;
            text-shadow: 0 0 15px #00ff66;
        }

        .typing {
            margin-top: 25px;
            color: #888;
            font-size: 18px;
        }

        .cursor {
            color: #00ff66;
            animation: blink 0.7s infinite;
        }

        @keyframes blink {
            50% {
                opacity: 0;
            }
        }

        .status {
            display: inline-block;
            margin-top: 30px;
            padding: 10px 18px;
            border: 1px solid #00ff66;
            color: #00ff66;
            box-shadow: 0 0 15px rgba(0,255,100,0.15);
        }

        /* SECTIONS */

        section {
            padding: 100px 8%;
            max-width: 1200px;
            margin: auto;
        }

        .section-title {
            color: #00ff66;
            font-size: 30px;
            margin-bottom: 35px;
            text-shadow: 0 0 10px rgba(0,255,100,0.5);
        }

        .section-title::before {
            content: "> ";
            color: #fff;
        }

        /* ABOUT */

        .about-box {
            padding: 30px;
            background: rgba(0,255,
